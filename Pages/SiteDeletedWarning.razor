@page "/SiteDeletedWarning"
@using MatBlazor
@using testing_app.Data

<MatSubtitle1 TValue = "string" Label="Are you sure you want to continue. Data will not be saved"></MatSubtitle1>
@code {
    [Parameter]
    public SiteSchedule itemDeleted { get; set; } = new SiteSchedule();
}